---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootttttddddd                  # Ingress name
  labels:
    app: springbootttttddddd
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootttttddddd.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootttttddddd               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
